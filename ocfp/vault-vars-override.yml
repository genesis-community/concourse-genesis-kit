---
meta:
  concourse_vault:
    url:                  (( vault meta.vault "/vault:url"))
    token:                (( vault meta.vault "/vault:token"))
    shared_path:          "shared"
    path_prefix:          "/concourse"
    insecure_skip_verify: false
    auth_backend_max_ttl: ~
