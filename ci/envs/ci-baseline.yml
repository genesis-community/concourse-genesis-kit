---
kit:
  features:
    - full
    - self-signed-cert

genesis:
  env:      ci-baseline
  bosh_env: genesis-ci

params:
  concourse_vm_type: large
  external_domain:   concourse.example.com
  num_web_nodes:     1

  availability_zones: [z1]
