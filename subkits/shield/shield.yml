---
instance_groups:
- name: db
  jobs:
  - name: shield-agent
    release: shield
    properties:
      shield:
        agent:
          daemon_public_key: (( vault params.shield_key_vault_path ))
  - name: agent-pgtools
    release: shield

releases:
- name: shield
  version: latest
